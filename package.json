{
  "name": "popscribe",
  "private": true,
  "version": "0.0.1",
  "description": "A cms using react, graphql and modern tools",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "lerna run --stream watch --no-private",
    "build": "lerna run --stream build --no-private"
  },
  "keywords": [
    "cms",
    "react",
    "graphql",
    "node"
  ],
  "author": "Alleo Indong",
  "license": "MIT",
  "devDependencies": {
    "jest": "^24.8.0",
    "lerna": "^3.15.0"
  },
  "workspaces": [
    "packages/*"
  ]
}
